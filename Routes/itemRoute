const express = require('express');

const router = express.Router();

const itemController = require("../Controllers/item_controller");

router.get('/item/new',itemController.create);

module.exports = router;